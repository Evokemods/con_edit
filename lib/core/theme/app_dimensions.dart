/// Application dimension constants.
///
/// Centralized spacing, sizing, and dimension definitions.
class AppDimensions {
  AppDimensions._();

  // Padding
  /// Extra small padding (4.0).
  static const double paddingXS = 4.0;

  /// Small padding (8.0).
  static const double paddingSmall = 8.0;

  /// Medium padding (16.0).
  static const double paddingMedium = 16.0;

  /// Large padding (24.0).
  static const double paddingLarge = 24.0;

  /// Extra large padding (32.0).
  static const double paddingXL = 32.0;

  // Border radius
  /// Small border radius (4.0).
  static const double radiusSmall = 4.0;

  /// Medium border radius (8.0).
  static const double radiusMedium = 8.0;

  /// Large border radius (12.0).
  static const double radiusLarge = 12.0;

  /// Extra large border radius (16.0).
  static const double radiusXL = 16.0;

  // Icon sizes
  /// Small icon size (16.0).
  static const double iconSmall = 16.0;

  /// Medium icon size (24.0).
  static const double iconMedium = 24.0;

  /// Large icon size (32.0).
  static const double iconLarge = 32.0;

  // Button heights
  /// Small button height (32.0).
  static const double buttonHeightSmall = 32.0;

  /// Medium button height (40.0).
  static const double buttonHeightMedium = 40.0;

  /// Large button height (48.0).
  static const double buttonHeightLarge = 48.0;

  // Input field heights
  /// Standard input field height (40.0).
  static const double inputHeight = 40.0;

  /// Large input field height (48.0).
  static const double inputHeightLarge = 48.0;

  // Spacing
  /// Extra small spacing (4.0).
  static const double spacingXS = 4.0;

  /// Small spacing (8.0).
  static const double spacingSmall = 8.0;

  /// Medium spacing (16.0).
  static const double spacingMedium = 16.0;

  /// Large spacing (24.0).
  static const double spacingLarge = 24.0;

  /// Extra large spacing (32.0).
  static const double spacingXL = 32.0;
}
